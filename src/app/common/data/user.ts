export class User {
    public id: number = Math.random();
    public username: string;
    public firstName:string;
    public password: string;
    public email: string;
     constructor(
    ){}
}
